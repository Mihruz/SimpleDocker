services:
  server:
    build:
      context: ../Part5
      dockerfile: Dockerfile
#    networks:
#      - mynetwork

  nginx_proxy:
    image: nginx
#    build: .
    ports:
      - 80:8080
    volumes:
      - ./nginx_proxy/nginx.conf:/etc/nginx/nginx.conf
#    networks:
#      - mynetwork  #
#    depends_on:
#      - server  #

#networks:
#  mynetwork:  # Создаем собственную сеть
 #   driver: bridge